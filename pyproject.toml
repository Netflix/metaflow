[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "metaflow"
version = "1.0.0"

# This print statement won't execute during pip install
# Need a different approach